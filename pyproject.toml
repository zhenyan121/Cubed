[project]
name = "cubed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
    "pillow>=12.2.0",
    "pytomlpp>=1.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
]
